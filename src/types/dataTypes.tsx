export interface ContactInformationType {
  facebook: string;
  youtube: string;
  phoneNumber: string;
  contactAddress: string;
}

export interface HomepageSliderType {
  id: string;
  image: string;
}
